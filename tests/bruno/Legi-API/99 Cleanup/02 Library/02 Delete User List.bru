meta {
  name: Delete User List
  type: http
  seq: 2
}

delete {
  url: {{libraryBaseUrl}}/api/v1/library/lists/{{listId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{accessToken}}
}
