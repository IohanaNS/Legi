meta {
  name: Remove Book From Library
  type: http
  seq: 5
}

delete {
  url: {{libraryBaseUrl}}/api/v1/library/{{userBookId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{accessToken}}
}
