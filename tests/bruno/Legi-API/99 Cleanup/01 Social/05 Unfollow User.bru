meta {
  name: Unfollow User
  type: http
  seq: 5
}

delete {
  url: {{socialBaseUrl}}/api/v1/social/follows/{{followingUserId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{accessToken}}
}
