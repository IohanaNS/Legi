meta {
  name: Delete Post Comment
  type: http
  seq: 2
}

delete {
  url: {{socialBaseUrl}}/api/v1/social/comments/{{postCommentId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{accessToken}}
}
